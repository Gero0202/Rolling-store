/**
 * 🧪 Router para pruebas de webhook de Mercado Pago.
 * Provee un endpoint para simular la recepción de notificaciones (webhooks)
 * y verificar su estructura y contenido durante el desarrollo.
 */

// import { Router } from "express";

// const routerMpTest = Router();

// routerMpTest.post("/webhooktest", (req, res) => {
//   console.log("✅ Webhook de prueba recibido!");
//   console.log("Headers:", req.headers);
//   console.log("Body:", req.body);
//   res.sendStatus(200); 
// });

// export default routerMpTest;
